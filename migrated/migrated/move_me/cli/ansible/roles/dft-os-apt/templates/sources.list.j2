{% for repo in dft_os_apt.sources_list_content %}
{%if repo.deb is not defined or repo.deb %}
deb {{ repo.url }} {{ repo.suite }} {{ repo.sections }}
{% endif %}
{%if repo.debsrc is defined and repo.debsrc %}
deb-src {{ repo.url }} {{ repo.suite }} {{ repo.sections }}
{% endif %}
{% endfor %}
