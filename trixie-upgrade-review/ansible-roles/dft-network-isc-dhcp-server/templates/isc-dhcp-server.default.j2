# This Ansible managed file and has been automatically generated on {{ ansible_date_time.date }} {{ ansible_date_time.time }}
# during deployment of role {{ role_name }} to host {{ ansible_host }}

# Therefore, please do not edit this file by hand. You should instead update the inventory
# and redeploy roles on {{ ansible_host }}. When being redeployed by ansible-playbook, managed
# files will be overriden. Thus, any local and manual modification will be lost.
# It's up to the sysadmin to update the inventory files first.
# New variables declared in inventory will be added, undeclared ones (or no longer declared) removed.

# Defaults for isc-dhcp-server (sourced by /etc/init.d/isc-dhcp-server)

{% if dft_network_isc_dhcp_server.default is defined %}
# Path to dhcpd's config file (default: /etc/dhcp/dhcpd.conf).
{% if dft_network_isc_dhcp_server.default.dhcpdv4_conf is defined %}
DHCPDv4_CONF={{ dft_network_isc_dhcp_server.default.dhcpdv4_conf }}
{% else %}
DHCPDv4_CONF=/etc/dhcp/dhcpd.conf
{% endif %}
{% if dft_network_isc_dhcp_server.default.dhcpdv6_conf is defined %}
DHCPDv4_PID={{ dft_network_isc_dhcp_server.default.dhcpdv6_conf }}
{% else %}
DHCPDv6_CONF=/etc/dhcp/dhcpd6.conf
{% endif %}

# Path to dhcpd's PID file (default: /var/run/dhcpd.pid).
{% if dft_network_isc_dhcp_server.default.dhcpdv4_pid is defined %}
DHCPDv4_PID={{ dft_network_isc_dhcp_server.default.dhcpdv4_pid }}
{% else %}
DHCPDv4_PID=/var/run/dhcpd.pid
{% endif %}
{% if dft_network_isc_dhcp_server.default.dhcpdv6_pid is defined %}
DHCPDv6_PID={{ dft_network_isc_dhcp_server.default.dhcpdv6_pid }}
{% else %}
#DHCPDv6_PID=/var/run/dhcpd6.pid
{% endif %}

# Additional options to start dhcpd with.
#	Don't use options -cf or -pf here; use DHCPD_CONF/ DHCPD_PID instead
{% if dft_network_isc_dhcp_server.default.options is defined %}
OPTIONS="{{ dft_network_isc_dhcp_server.default.options }}"
{% else %}
OPTIONS=""
{% endif %}

# On what interfaces should the DHCP server (dhcpd) serve DHCP requests?
#	Separate multiple interfaces with spaces, e.g. "eth0 eth1".
{% if dft_network_isc_dhcp_server.default.interfacesv4 is defined %}
INTERFACESv4="{{ dft_network_isc_dhcp_server.default.interfacesv4 }}"
{% else %}
INTERFACESv4=""
{% endif %}
{% if dft_network_isc_dhcp_server.default.interfacesv6 is defined %}
INTERFACESv6="{{ dft_network_isc_dhcp_server.default.interfacesv6 }}"
{% else %}
INTERFACESv6=""
{% endif %}
{% else %}
#DHCPDv4_CONF=/etc/dhcp/dhcpd.conf
#DHCPDv6_CONF=/etc/dhcp/dhcpd6.conf

# Path to dhcpd's PID file (default: /var/run/dhcpd.pid).
#DHCPDv4_PID=/var/run/dhcpd.pid
#DHCPDv6_PID=/var/run/dhcpd6.pid

# Additional options to start dhcpd with.
#	Don't use options -cf or -pf here; use DHCPD_CONF/ DHCPD_PID instead
#OPTIONS=""

# On what interfaces should the DHCP server (dhcpd) serve DHCP requests?
#	Separate multiple interfaces with spaces, e.g. "eth0 eth1".
INTERFACESv4=""
INTERFACESv6=""

{% endif %}
